<?php

class DevsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('devs')->truncate();

		$devs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('devs')->insert($devs);
	}

}
